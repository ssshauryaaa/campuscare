version: '3.8'

services:
  team-a:
    build: .
    ports:
      - "3001:3000"
    environment:
      - TEAM_ID=team-a
    restart: unless-stopped

  team-b:
    build: .
    ports:
      - "3002:3000"
    restart: unless-stopped

  team-c:
    build: .
    ports:
      - "3003:3000"
    restart: unless-stopped

  team-d:
    build: .
    ports:
      - "3004:3000"
    restart: unless-stopped